# ВОРОЖБИТОВ

Telegram-магазин мерча и одежды: один бот + Telegram Mini App в общей стилистике бренда.
Витрина сделана mobile-first: её можно открыть в браузере для предпросмотра или подключить к кнопке Web App в Telegram.

Обновление от 9 сентября 2026: [интерфейс, исправления, проверка и статус медиа](PROJECT-REFRESH.md). В текущем каталоге выбран `campaign-v6.mp4` (19,63 с): живые действия в исходной скорости, короткие предметные вставки и серый металлический финал. 30-секундный фон приветствия используется отдельно. Музыка основана на исходном тизере и существующем звуке серого финала v4.

## Что изменено

### Бренд и визуальная система

Концепция опирается на публичное описание Никиты Ворожбитова как автора контента про реальные жизненные истории, спорт, мотивацию и развитие. Визуальный перевод этого образа в магазин — тёмная утилитарная база, бетонная фактура, красный «сигнал», крупная типографика, короткие прямые CTA и ощущение ограниченного выпуска.

В магазин намеренно не переносились шок-контент, политические сообщения, 18+ материалы, реклама казино или конфликтная риторика. Магазин остаётся про вещи, качество, размеры и заказ.

Перед публичным запуском нужно подтвердить у Никиты/правообладателя разрешение на название, логотип, фото, слоганы и продажу мерча. Сгенерированные изображения в `miniapp/assets/` — визуальные мокапы для прототипа, не фотографии реального товара.

### Telegram-бот

Меню в чате собрано по правилам из [TELEGRAM-MENU.md](TELEGRAM-MENU.md): шесть разделов
в две колонки, единый стиль карточек вещи и покупки, стабильные «← Назад» и «🏠 Главная»,
отдельный пульт команды. Системная кнопка «Меню» открывает команды бота, а не Mini App.

- `/start` — ролик выпуска для новых, обложка бренда, интерес и главное меню; `/menu`, `/catalog`, `/orders`, `/support`, `/help` — те же экраны, что и кнопки;
- главное меню: каталог, мои покупки, образы, ролик, кабинет, поддержка; незакрытая оплата поднимается отдельной широкой кнопкой «Продолжить оплату»;
- навигация кнопками меняет экран на месте (`editMessageText`) — чат не тонет в ленте сообщений;
- витрина по разделам с реальным количеством вещей, карточки вещей альбомом с честными подписями кадров, размеры и замеры рядом с выбором, лист ожидания — в кабинете экран «Жду размер», где каждую запись можно снять;
- карточка покупки отвечает на три вопроса отдельными строками: оплата, сборка, доставка; кнопка оплаты доступна, только пока счёт действительно можно оплатить, и продолжает прежний счёт;
- поддержка: «Вопрос по покупке» уходит менеджеру сразу с номером, составом и статусом — переспрашивать не приходится;
- согласие на обработку данных до сохранения телефона, оформление с прогрессом «шаг 1 из 2 / шаг 2 из 2»;
- покупка вещи через бота или витрину; оплата сразу (Лава / крипта / звёзды); «Мои покупки» и отмена до оплаты;
- ссылка «привести друга» `?start=ref<id>` и приоритет на выпуск;
- образы, канал бренда, сетка размеров;
- пульт владельца: сводка, покупки, лист ожидания, добавление вещи, топ рефералов, доступ команды, история розыгрышей, возврат размера, рассылки; экспорт CSV и перечитывание каталога — команды;
- доступ команды: владелец назначает админов `/grant id или @username` и снимает `/revoke id` (или кнопкой на экране «Доступ команды»); админ хранится в базе и получает пульт без перезапуска, но сам никого не назначает;
- главное меню `/start` у владельца и админов показывает кнопку «Управление магазином» — пульт открывается из начального экрана, а журнал рассылок собран командой `/reports`;
- деньги и дайджест: `/money` (всего, сегодня, за 7 дней, средний чек, возвраты) и `/digest` (подтверждения, оплаты, дефицит размеров, возвраты) — оба входа также кнопками из сводки; ресток дефицита — кнопкой прямо в экране листа ожидания;
- витрина кнопками: `/shelf` скрывает и возвращает вещи без команд `/hide` `/show`; карточка покупки открывает карточку клиента (контакт, согласие, деньги, привод); порог приоритета розыгрыша меняется кнопками «Порог −/+» в экране розыгрышей и хранится в kv;
- рутина возврата и повтор рассылки: `/refunds` собирает возвраты очередью с причиной кнопкой и отметкой «выполнен», `/again` поднимает прошлую рассылку в предпросмотр одним словом;
- поддержка и анонсы: в экране поддержки быстрые ответы кнопками, набор правит владелец в `/faqs` (добавить, править, убрать); после публикации вещи кнопка «Анонс →» собирает текст рассылки из карточки и кладёт его в черновик `/broadcast`;
- брошенные оплаты: `/unpaid` (и кнопка из дайджеста) показывает начатые, но не оплаченные покупки с суммой и возрастом; «Напомнить →» отправляет покупателю карточку с кнопкой оплаты, повтор — не чаще раза в 12 часов;
- все изменения каталога сохраняются в `data/catalog.json` без перезапуска;
- кнопка «Открыть витрину ↗» внутри чата, если задан `WEBAPP_URL`.

### Mini App

- boot-штамп «Сила и честь», приветственный экран с фото и текстом, затем витрина;
- фото магазина (бетон / неон ВОРОЖБИТОВ), вешалки, lookbook и полевые кадры;
- поиск, категории, сортировка, фильтр размера и наличия, сохранённые вещи;
- обзор вещи с разных сторон в карточке (тяни фото влево-вправо), вкладка «фото»;
- фирменные вещи с подписями в голосе бренда: «Сила и честь», 1993, «Один в поле», красная V;
- карточка товара: галерея, материал, посадка, детали, количество, размерная сетка и подсказка по росту;
- профиль: имя, телефон, город, адрес, доставка и размер — подставляются в заявку;
- заявки в профиле витрины: статусы из бота (ждёт оплаты / оплачена / подтверждена / отменена), отмена до оплаты;
- лист ожидания размера, «с этим берут», шаринг канала;
- заявка: количество, город, СДЭК / Яндекс / самовывоз, комментарий, согласие;
- в Telegram заявка уходит на `/api/checkout`; при сетевой ошибке повтор использует тот же ключ, корзина сохраняется; лист ожидания — через `/api/waitlist`; haptic + BackButton;
- бот повторно проверяет на сервере product id, активность товара, размер, телефон и количество — цена/описание из браузера не считаются доверенными;
- в браузерном предпросмотре можно собрать корзину; для отправки требуется открыть Mini App из бота. Без подтверждения сервера корзина сохраняется.

## Архитектура

```text
.
├── bot.py                    # Telegram polling, SQLite, API Mini App, админка, оплата
├── payments.py               # Лава, Crypto Pay, звёзды
├── catalog.json              # seed-каталог
├── miniapp/
│   ├── index.html            # экран магазина
│   ├── styles.css            # inline-friendly визуальная система без CDN
│   ├── refinement.css        # адаптивная подача, галерея и доступность
│   ├── app.js                # каталог, корзина, Web App bridge
│   ├── shop-core.js          # проверка сохранённых данных и фильтры
│   ├── viewer3d.js           # фото-обзор: тяни кадры влево-вправо
│   └── assets/               # demo product/editorial images
├── preview_server.py         # локальный просмотр Mini App без BOT_TOKEN
├── tools/menu_preview.py     # предпросмотр native-меню бота в виде чата
├── data/                     # создаётся при запуске; в git не попадает
├── Dockerfile
└── compose.yaml
```

Один Python-процесс отдаёт:

- `/` или `/app` — Mini App;
- `/api/catalog` — активный каталог из памяти бота;
- `/api/checkout` — заявка + ссылки на оплату (HMAC initData);
- `/api/payments/lava` и `/api/payments/crypto` — вебхуки оплаты;
- `/health` — health-check;
- `/assets/*` — изображения и фронтенд Mini App.

Поэтому изменения товара через `/add`, `/hide`, `/show`, `/reload` видны Mini App без второго сервиса.

## Локальный запуск Mini App

Не нужен Telegram-токен:

```bash
python3 preview_server.py
```

Открыть `http://localhost:4173`.

Проверка API:

```bash
curl http://localhost:4173/health
curl http://localhost:4173/api/catalog
```

## Запуск бота и Mini App

1. Создай бота в `@BotFather` и получи токен.
2. Поставь аватар: `@BotFather` → бот → **Edit Bot** → **Edit Botpic**. Файл `miniapp/assets/bot-avatar.jpg` (портрет) или `miniapp/assets/bot-mark.jpg` (знак V). Квадрат, JPEG.
3. Узнай цифровые ID админов и чата менеджера.
4. Создай конфигурацию:

```bash
cp .env.example .env
```

Минимальные значения:

```env
BOT_TOKEN=...
ADMIN_IDS=123456789
# ADMIN_IDS — владельцы: только они назначают админов (/grant, /revoke).
# Админы добавляются из бота и живут в таблице admins — перезапуск не нужен.
MANAGER_CHAT_ID=123456789
CHANNEL_URL=https://t.me/+XufFz8GGR0o3Njky
WEBAPP_URL=https://shop.example.com
BRAND_NAME=ВОРОЖБИТОВ
PRIVACY_URL=https://example.com/privacy
LAVA_SHOP_ID=
LAVA_SECRET_KEY=
LAVA_HOOK_KEY=
CRYPTO_PAY_TOKEN=
STARS_ENABLED=1
STARS_RUB_PER_STAR=2
```

`WEBAPP_URL` должен быть доступен по HTTPS и указывать на тот же домен/порт, где работает `bot.py`. Если оставить поле пустым, бот продолжит работать, но кнопка «Открыть витрину ↗» в главном меню не появится. Системная кнопка «Меню» в шапке чата от `WEBAPP_URL` не зависит: она всегда открывает команды бота.

**Важно про фото и видео в чате.** Telegram скачивает картинки товаров по публичной ссылке, которая строится из `WEBAPP_URL`. Пока домен не поднят, карточки уходят текстом без фото — на витрине при этом всё видно. Ролик выпуска устроен иначе: он загружается файлом с диска (`miniapp/assets/video/teaser.mp4`, 3 МБ), поэтому работает даже без домена. После первой отправки Telegram возвращает `file_id`, бот кеширует его в таблице `kv` и дальше шлёт ролик мгновенно, не перезаливая файл.

Оплата сразу после заявки. Пустой ключ — этот способ не показывается. Звёзды работают без ключа Лавы/крипты (`STARS_ENABLED=1`). В Crypto Bot укажи вебхук `https://твой-домен/api/payments/crypto`. Лава берёт `hookUrl` из счёта. Reverse proxy должен отдавать эти пути на `bot.py`.

Звёзды Telegram задуманы под цифровые товары; для одежды это серая зона правил площадки — заказчик всё равно включает все три способа.

Запуск без зависимостей:

```bash
python3 bot.py
```

Docker:

```bash
docker compose up -d --build
```

В продакшене поставь reverse proxy с TLS перед портом `8080`. Compose публикует backend только на `127.0.0.1:8080`. Проксируй все пути через `location /`, включая `/api/checkout`, `/api/pay`, `/api/my-orders`, `/api/my-orders/cancel`, `/api/waitlist`, `/api/payments/lava`, `/api/payments/crypto`, JS/CSS и медиа. Telegram не принимает обычный HTTP для Web App.

Если nginx работает на хосте, `proxy_pass http://127.0.0.1:8080;`. Обязательно перезаписывай заголовок `proxy_set_header X-Real-IP $remote_addr;`. В `TRUSTED_PROXY_IPS` укажи точный адрес прокси, который виден приложению: при Docker это может быть адрес шлюза Docker, а не `127.0.0.1`. Непроверенные forwarded-заголовки игнорируются. Для nginx в отдельном контейнере подключайся к `bot:8080` во внутренней сети Compose.

До первого запуска создай `data` и дай UID 10001 право записи: `mkdir -p data && sudo chown 10001:10001 data`. Для уже работающей базы сначала проверь текущие права и сделай резервную копию; не меняй владельца вслепую.

## Каталог

Рабочая копия создаётся при первом запуске:

```text
data/catalog.json
```

`catalog.json` в корне — исходный каталог. При обновлении работающего магазина не удаляй `data/catalog.json`: в нём могут быть актуальные размеры, доступность и изменения администратора. Переноси нужные визуальные поля по `id` товара, сохраняя рабочие цены, размеры и статусы. Замена seed-файла сама по себе не обновляет рабочий каталог.

Основные поля товара:

```json
{
  "id": "drop-tee-001",
  "category": "drop",
  "name": "CORE TEE",
  "price": "4 900 ₽",
  "sizes": ["S", "M", "L", "XL"],
  "description": "...",
  "photo_url": "",
  "image": "assets/base-tee.jpg",
  "badge": "ВЫПУСК",
  "material": "100% хлопок · 240 г/м²",
  "fit": "Свободный крой",
  "details": ["..."],
  "stock_label": "Осталось мало",
  "active": true
}
```

- `price` — одно число с необязательной валютой (`4 900 ₽`, `4900`). Строку, из
  которой нельзя однозначно вывести сумму, бот пишет в лог как ошибку и не
  выставляет по такому товару счёт;
- `photo_url` — публичный URL для отправки фото в Telegram-боте;
- `image` — путь фронтенд-ассета Mini App;
- `active: false` скрывает вещь из бота и Mini App;
- `id` и размеры не должны содержать двоеточие: они участвуют в callback-data Telegram.

Реальные фото товара лучше загрузить в CDN/объектное хранилище, указать в `photo_url`, а локальные demo-изображения заменить в `image`.

## Админ-команды

- `/admin` — пульт управления магазином;
- `/add` — мастер «новая вещь»: шаги со счётчиком, а перед публикацией показывает карточку так, как её увидит покупатель;
- `/hide ID` / `/show ID` — скрыть или вернуть товар;
- `/reload` — перечитать `catalog.json`;
- `/stats` — сводка: оплаченные покупки и сумма, ожидающие оплаты, возвраты, база, лист ожидания;
- `/orders` — покупки одним списком с группами; карточка и смена этапа одним главным действием открываются по номеру;
- `/waitlist` — лист ожидания;
- `/restock ID SIZE` — уведомить ожидающих размер;
- `/broadcast ТЕКСТ` — сегментированная рассылка: сегменты в две колонки, предпросмотр с числом получателей;
- `/top`, `/giveaway N`, `/export` — referral, розыгрыш и CSV.

Рассылка идёт фоновым потоком: бот продолжает отвечать, отчёт о доставке приходит
в тот же чат по завершении.

Цена в `/add` принимается только одним числом (`9 900 ₽`, `9900`, `от 9900`).
Диапазоны и приписки в скобках отклоняются: из такой строки нельзя однозначно
посчитать сумму счёта. Там же ограничены длина названия (48), раздела (32),
описания (1200) и размера (18 байт): из более длинных значений не собрать экран в
лимитах Telegram — подпись кнопки ограничена 64 знаками, `callback_data` 64
байтами, сообщение 4096 знаками.

Каталог проверяется до записи, а не после: `Catalog.parse` разбирает будущий файл
целиком, и только потом он попадает в память и на диск. Отвергнутая публикация не
портит `catalog.json`, `/reload` с битым файлом продолжает работать с последним
хорошим, а старт с битым файлом завершается кодом 2 с внятной причиной. Любой
отказ каталога сообщается админу в чат — молчания на этом пути нет.

## Проверка

```bash
python3 -m py_compile bot.py preview_server.py payments.py
node --check miniapp/app.js
python3 -m unittest -v
python3 -m unittest test_bot.NativeMenuTests -v   # структура меню и карточек в чате
python3 smoke_test.py                             # весь диалог текстом, как его видит покупатель
python3 tools/menu_preview.py --serve 8099        # те же экраны в виде чата, без токена бота
```

## Перед боевым запуском

- заменить demo-товары, цены, размеры и изображения на подтверждённые;
- проверить право на использование имени «ВОРОЖБИТОВ» и фирменных элементов;
- добавить публичную политику обработки данных и реквизиты продавца;
- подключить оплату после выбора юрлица, провайдера, возвратов и доставки;
- загрузить аватар бота в `@BotFather` (`miniapp/assets/bot-avatar.jpg`);
- проверить, что `/start` присылает фото зала, а не один текст;
- проверить Web App в Telegram на реальном HTTPS-домене;
- проверить, что менеджер получает город, телефон и все позиции корзины;
- настроить бэкапы `data/bot.sqlite3` и `data/catalog.json`;
- прогнать тестовую заявку на отдельном боте;
- не использовать чужие изображения людей и реальные фото без разрешения.

## Публичные ориентиры для тона

- [YouTube — описание авторского канала Никиты Ворожбитова](https://www.youtube.com/@nikita_vorogbitov/about)
- [Telegram — канал «ВОРОЖБИТОВ»](https://t.me/+XufFz8GGR0o3Njky)
- [TGStat — описание и публичная статистика канала](https://tgstat.ru/en/channel/XufFz8GGR0o3Njky)


## Исправления аудита 8 сентября 2026

- Checkout сохраняет платёж, все позиции, результат и задания уведомлений одной транзакцией. Повтор `(user_id, request_id)` возвращает тот же результат; изменение тела с прежним ключом отклоняется. Старые ключи заявок не создают дубликаты — ответ направляет в «Мои заявки».
- Позиции с ошибочной или нулевой ценой не допускаются к checkout. Рублёвая сумма и Stars фиксируются при создании.
- Клиентская отмена доступна только до оплаты и отменяет все позиции общей заявки. Новые счета по ней не создаются. Ранее выданный внешний счёт может быть оплачен у провайдера: такой платёж получает `refund_required`, магазин не возобновляет исполнение автоматически, клиент и менеджер получают уведомления.
- Ручная отметка оплаты администратором фиксирует оплату всей связанной корзины. Отмена администратором оплаченной позиции требует ручной сверки/возврата.
- Уведомления хранятся в SQLite и повторяются фоновым потоком после ошибок или перезапуска. Доставка — как минимум один раз: при аварии после отправки, но до записи результата возможно повторное сообщение; повторного заказа это не создаёт.
- Повторяются только временные сбои. Если собеседник заблокировал бота или сообщение недоставляемо в принципе, задача снимается с очереди, а пользователь помечается заблокированным — рассылки в закрытый чат больше не уходят.
- Лист ожидания использует подписанный HTTP API, `/app/` перенаправляется на `/`, память лимитера ограничена.

Проверки: `python3 -m unittest -v`, `python3 smoke_test.py`, `node --test test_checkout_client.cjs`. Smoke-тест блокирует внешнюю сеть и падает при неверных результатах. Workflow `.github/workflows/tests.yml` запускает проверки на Python 3.11 и 3.13.

Порядок обновления, сверка старых данных и откат: [OPERATIONS.md](OPERATIONS.md).
